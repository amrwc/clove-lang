p = 3

function blah(start, fin) {

	function zot(x, y) {
	   	function zaz(r) {
		   return p + x * r
		}

		return x * y + zaz(y)
	}

	for (i=start; i<=fin; i=i+1) {
		write(zot(i, p))
	}

	write(p * 1000)
}

blah(5, 10)
