// Object keys reuse


const object = {}

// Populate the structure
for (let i = 100; i < 110; i++) {
  object[i] = random(0, i)
}
log('object: ', object)
const keys = object->keys()
log('keys: ', keys)

//let bob = keys[2]

log('object[keys[2]]: ', object[keys[2]])
//log(object[bob])
