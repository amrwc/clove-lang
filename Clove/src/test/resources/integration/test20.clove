// Test token aliases
log("This text") // write := log, wyswietl, pokaz
log("          has been")
log("                   printed using")
wyswietl("                                 different aliases")
pokaz("                                                   for log().")

log()

funkcja foo(str) {
	log(str)
}
foo("function := func, fn, funkcja")

zakoncz() // quit := zakoncz, wyjdz

log("This won't be printed out.")
