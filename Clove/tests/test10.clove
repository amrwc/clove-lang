let p = 3

function blah(start, fin) {

	function zot(x, y) {
	   	function zaz(r) {
		   return p + x * r
		}

		return x * y + zaz(y)
	}

	for (let i=start; i<=fin; i=i+1) {
		log(zot(i, p))
	}

	log(p * 1000)
}

blah(5, 10)
